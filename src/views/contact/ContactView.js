import React from 'react';
import NavBar from '../../components/NavBar';

const ContactView = () => {
    return(
        <nav>
            <NavBar/>
            <h1>Contact</h1>
        </nav>
    )
} 
export default ContactView;